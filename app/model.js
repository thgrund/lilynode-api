class Model {
  constructor (format, resolution, binaryPath, content) {
    this.format = format;
    this.resolution = resolution;
    this.binaryPath = binaryPath;
    this.content = content;
  }
}

module.exports = Model;